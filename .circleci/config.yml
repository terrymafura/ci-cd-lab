version: 2.1


jobs:
  say-hello:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: |
            yum install -y node

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
